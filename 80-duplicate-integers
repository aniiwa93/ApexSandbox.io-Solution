public Boolean containsDuplicates(List<Integer> nums) {
    Set<Integer> nums2 = Set<Integer>(nums);
    if (nums2.size() == nums.size()){
        return false;
    }
    else {
        return true;
    }
}