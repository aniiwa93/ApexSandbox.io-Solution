public static Integer arraySum(List<Integer> numbers) {
    Integer arraySum = 0;
    for(i = 0; i <= numbers.size(); i++){
        Integer num = numbers[i];
        arraySum += num;

    }
    return arraySum;
}